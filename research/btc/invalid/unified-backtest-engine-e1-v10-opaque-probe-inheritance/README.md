# Rejected E1-v10 preimplementation identity

Experiment `btc-unified-backtest-engine-e1-v10` failed independent preflight before code and is
immutable negative evidence. Its hashes, archive closure, blind-oracle boundary, absent paths and
three E0 semantic corrections passed. It nevertheless referred to executable V8/V9 probes that
the clean-room implementer was forbidden to read and did not reproduce every probe by value.

The exact rejected plan and contract are preserved here. They must not be imported, implemented,
repaired, or used as clean-room inputs. A successor must state every inherited executable probe
directly in its own implementer-readable contract without relying on prohibited archives.
