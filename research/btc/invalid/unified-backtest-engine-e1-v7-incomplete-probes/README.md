# Invalid E1-v7 identity — incomplete executable probes

E1-v7 failed independent preflight before any implementation. Its semantic prose contained the
required severe-bound and liquidation/output requirements, but three of them were not also frozen
as distinct mandatory executable probes: worse-than-frozen severe-path rejection before mutation;
exact per-ledger E0 field-set and account/invalidation checks; and rejection of placeholder or
mismatched liquidation mark, rule and margin lineage.

The exact frozen plan and contract are preserved unchanged. They must not be implementation inputs,
copied, repaired or treated as passed. No E1-v7 module, test, snapshot, oracle, historical data,
strategy result, credential, external service or executable action was created or accessed.
