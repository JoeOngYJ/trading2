# Rejected E1-v13 preimplementation identity

E1-v13 failed independent preflight before code because `pair_l2_primary` combined a qualified-L2
250 ms adapter with the incompatible zero-latency candle scenario identity. All other integrity,
archive, oracle, ownership and semantic checks passed. Exact V13 plan, contract and six fixture
authorities are preserved as immutable negative evidence and are not implementation inputs.
