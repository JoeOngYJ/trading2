# Rejected E1-v12 preimplementation identity

Experiment `btc-unified-backtest-engine-e1-v12` failed independent preflight before code. Its
integrity, archive closure, oracle separation, absent paths and modular economic semantics passed,
but its named RunSpecs did not freeze exact contents or hashes before implementation, and K04/K06
had overlapping primary test owners. Integration also claimed the semantic probes instead of a
separate wiring-only set.

The exact V12 plan and contract are immutable negative evidence. They must not be imported,
implemented, repaired or used as clean-room inputs. A successor must provide pre-existing
checksummed RunSpec/support authorities and exactly one primary owner for every semantic probe.
